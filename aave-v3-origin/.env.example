# Copy to .env and fill in values. Foundry loads .env from the repo root.
# Do not commit .env.

# Sepolia HTTPS RPC (Alchemy, Infura, public node, etc.)
# Used by: forge ... --rpc-url sepolia
RPC_SEPOLIA=
